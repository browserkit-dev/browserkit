# session state and credentials — never commit
*.storage-state.json
.credentials/
.env
.env.local
.env.production

# runtime
node_modules/
dist/
build/
__pycache__/
*.pyc
.venv/

# agent-check output — runtime, not for commits
agent-check-results.json

# OS
.DS_Store

# adapters live in their own repos — excluded from the core repo
packages/adapter-hackernews/
packages/adapter-linkedin/

# local MCP client configuration — machine-specific
.mcp.json
.cursor/mcp.json
